apiVersion: v1
kind: Service
metadata:
  name: staywell-frontend-service
  namespace: staywell-manager-dev
  labels:
    app: staywell-frontend
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: staywell-frontend
