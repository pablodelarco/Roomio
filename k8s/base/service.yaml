apiVersion: v1
kind: Service
metadata:
  name: staywell-frontend-service
  labels:
    app: staywell-frontend
    app.kubernetes.io/name: staywell-frontend-service
    app.kubernetes.io/part-of: staywell-manager
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: staywell-frontend
