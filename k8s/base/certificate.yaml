apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: staywell-tls
  labels:
    app.kubernetes.io/name: staywell-tls
    app.kubernetes.io/part-of: staywell-manager
spec:
  secretName: staywell-tls-secret
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
  - pablodelarco.com  # Will be overridden by overlays
  usages:
  - digital signature
  - key encipherment
  # Certificate renewal settings
  renewBefore: 720h  # Renew 30 days before expiration
  duration: 2160h    # 90 days validity
  # Subject settings
  subject:
    organizationalUnits:
    - "StayWell Manager"
    organizations:
    - "Pablo del Arco"
    countries:
    - "ES"
  # Private key settings
  privateKey:
    algorithm: RSA
    size: 2048
    rotationPolicy: Always
