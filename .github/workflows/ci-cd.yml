name: CI/CD Pipeline

on:
  push:
    branches: [ main, develop, staging ]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: roomio

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
    
    - name: Install dependencies
      run: npm ci
    
    - name: Run tests
      run: npm test
    
    - name: Build application
      run: npm run build:prod

  build-and-push:
    needs: test
    runs-on: ubuntu-latest

    permissions:
      contents: write
      packages: write
      actions: read
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        fetch-depth: 0  # Fetch full history to detect merge commits properly
    
    - name: Log in to Container Registry
      uses: docker/login-action@v3
      with:
        registry: ${{ env.REGISTRY }}
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}
    
    - name: Extract metadata
      id: meta
      uses: docker/metadata-action@v5
      with:
        images: ${{ env.REGISTRY }}/${{ github.repository_owner }}/${{ env.IMAGE_NAME }}
        tags: |
          type=ref,event=branch
          type=ref,event=pr
          type=sha,prefix=sha-
          type=raw,value=latest,enable={{is_default_branch}}
        flavor: |
          latest=auto
    
    - name: Build and push Docker image
      uses: docker/build-push-action@v5
      with:
        context: .
        push: true
        tags: ${{ steps.meta.outputs.tags }}
        labels: ${{ steps.meta.outputs.labels }}
    
    - name: Security scan with Trivy
      uses: aquasecurity/trivy-action@master
      with:
        image-ref: ${{ fromJSON(steps.meta.outputs.json).tags[0] }}
        format: 'table'
        exit-code: '0'
      continue-on-error: true

    - name: Security scan summary (JSON)
      uses: aquasecurity/trivy-action@master
      with:
        image-ref: ${{ fromJSON(steps.meta.outputs.json).tags[0] }}
        format: 'json'
        output: 'trivy-results.json'
        exit-code: '0'
      continue-on-error: true

    - name: Display security scan summary
      if: always()
      run: |
        echo "=== Security Scan Summary ==="
        echo "🔍 Scanned image: ${{ fromJSON(steps.meta.outputs.json).tags[0] }}"

        if [ -f trivy-results.json ]; then
          echo "✅ Security scan completed successfully"

          # Parse and display vulnerability counts
          if command -v jq &> /dev/null; then
            CRITICAL=$(jq '[.Results[]?.Vulnerabilities[]? | select(.Severity=="CRITICAL")] | length' trivy-results.json 2>/dev/null || echo '0')
            HIGH=$(jq '[.Results[]?.Vulnerabilities[]? | select(.Severity=="HIGH")] | length' trivy-results.json 2>/dev/null || echo '0')
            MEDIUM=$(jq '[.Results[]?.Vulnerabilities[]? | select(.Severity=="MEDIUM")] | length' trivy-results.json 2>/dev/null || echo '0')
            LOW=$(jq '[.Results[]?.Vulnerabilities[]? | select(.Severity=="LOW")] | length' trivy-results.json 2>/dev/null || echo '0')

            echo "📊 Vulnerability Summary:"
            echo "   🔴 Critical: $CRITICAL"
            echo "   🟠 High: $HIGH"
            echo "   🟡 Medium: $MEDIUM"
            echo "   🟢 Low: $LOW"

            if [ "$CRITICAL" -gt 0 ] || [ "$HIGH" -gt 0 ]; then
              echo "⚠️  High or Critical vulnerabilities found! Review the scan results above."
            else
              echo "✅ No Critical or High severity vulnerabilities found!"
            fi
          else
            echo "📄 Scan completed - see detailed results above"
          fi
        else
          echo "❌ Security scan failed or results not found"
          echo "This may be due to image availability issues or scan errors"
        fi

    - name: Detect merge commits and sync image tags
      id: detect_merge
      run: |
        # Check if this is a merge commit
        PARENT_COUNT=$(git cat-file -p HEAD | grep "^parent" | wc -l)
        COMMIT_MESSAGE=$(git log -1 --pretty=%B)
        IS_MERGE=false
        SOURCE_BRANCH=""

        echo "📊 Parent count: $PARENT_COUNT"
        echo "📝 Commit message: $COMMIT_MESSAGE"

        # A merge commit has 2 or more parents
        if [ "$PARENT_COUNT" -ge 2 ]; then
          echo "📋 Merge commit detected!"

          # Detect which branches are being merged based on current branch and commit message
          if [[ "${{ github.ref }}" == "refs/heads/staging" ]]; then
            # We're on staging, check if this is a merge from develop
            if [[ "$COMMIT_MESSAGE" =~ "develop" ]]; then
              IS_MERGE=true
              SOURCE_BRANCH="develop"
              echo "🔀 Confirmed: Merge from develop to staging"
            fi
          elif [[ "${{ github.ref }}" == "refs/heads/main" ]]; then
            # We're on main, check if this is a merge from staging
            if [[ "$COMMIT_MESSAGE" =~ "staging" ]]; then
              IS_MERGE=true
              SOURCE_BRANCH="staging"
              echo "🔀 Confirmed: Merge from staging to main"
            fi
          fi
        else
          echo "📋 Regular commit (not a merge)"
        fi

        echo "✅ is_merge=$IS_MERGE"
        echo "✅ source_branch=$SOURCE_BRANCH"
        echo "is_merge=$IS_MERGE" >> $GITHUB_OUTPUT
        echo "source_branch=$SOURCE_BRANCH" >> $GITHUB_OUTPUT

    - name: Sync image tags from source branch (for merges)
      if: steps.detect_merge.outputs.is_merge == 'true'
      run: |
        SOURCE_BRANCH="${{ steps.detect_merge.outputs.source_branch }}"

        if [[ "${{ github.ref }}" == "refs/heads/staging" ]] && [[ "$SOURCE_BRANCH" == "develop" ]]; then
          # Sync development tag to staging
          SOURCE_TAG=$(grep "newTag:" k8s/overlays/development/kustomization.yaml | awk '{print $2}')
          echo "📦 Syncing image tag from development to staging: $SOURCE_TAG"
          sed -i "s|newTag: .*|newTag: $SOURCE_TAG|" k8s/overlays/staging/kustomization.yaml
          echo "synced_tag=$SOURCE_TAG" >> $GITHUB_OUTPUT
        elif [[ "${{ github.ref }}" == "refs/heads/main" ]] && [[ "$SOURCE_BRANCH" == "staging" ]]; then
          # Sync staging tag to production
          SOURCE_TAG=$(grep "newTag:" k8s/overlays/staging/kustomization.yaml | awk '{print $2}')
          echo "📦 Syncing image tag from staging to production: $SOURCE_TAG"
          sed -i "s|newTag: .*|newTag: $SOURCE_TAG|" k8s/overlays/production/kustomization.yaml
          echo "synced_tag=$SOURCE_TAG" >> $GITHUB_OUTPUT
        fi

    - name: Update image tags in manifests (for regular commits)
      if: steps.detect_merge.outputs.is_merge == 'false'
      run: |
        # Get the commit SHA for unique tagging
        COMMIT_SHA=$(echo "${{ github.sha }}" | cut -c1-7)
        IMAGE_TAG="sha-${COMMIT_SHA}"

        echo "Commit SHA: $COMMIT_SHA"
        echo "Updating image tag to: $IMAGE_TAG"

        # Update Kustomize overlays based on branch
        if [[ "${{ github.ref }}" == "refs/heads/develop" ]]; then
          echo "Updating development deployment..."
          sed -i "s|newTag: .*|newTag: $IMAGE_TAG|" k8s/overlays/development/kustomization.yaml
        elif [[ "${{ github.ref }}" == "refs/heads/staging" ]]; then
          echo "Updating staging deployment..."
          sed -i "s|newTag: .*|newTag: $IMAGE_TAG|" k8s/overlays/staging/kustomization.yaml
        elif [[ "${{ github.ref }}" == "refs/heads/main" ]]; then
          echo "Updating production deployment..."
          sed -i "s|newTag: .*|newTag: $IMAGE_TAG|" k8s/overlays/production/kustomization.yaml
        fi

    - name: Commit and push updated manifests
      if: github.ref == 'refs/heads/develop' || github.ref == 'refs/heads/staging' || github.ref == 'refs/heads/main'
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"

        # Add the appropriate overlay file based on branch
        if [[ "${{ github.ref }}" == "refs/heads/develop" ]]; then
          git add k8s/overlays/development/kustomization.yaml
        elif [[ "${{ github.ref }}" == "refs/heads/staging" ]]; then
          git add k8s/overlays/staging/kustomization.yaml
        elif [[ "${{ github.ref }}" == "refs/heads/main" ]]; then
          git add k8s/overlays/production/kustomization.yaml
        fi

        if git diff --staged --quiet; then
          echo "No changes to commit"
        else
          # Determine commit message based on whether this was a merge or regular commit
          if [[ "${{ steps.detect_merge.outputs.is_merge }}" == "true" ]]; then
            SOURCE_BRANCH="${{ steps.detect_merge.outputs.source_branch }}"
            SYNCED_TAG=$(grep "newTag:" k8s/overlays/$([[ "${{ github.ref }}" == "refs/heads/staging" ]] && echo "staging" || echo "production")/kustomization.yaml | awk '{print $2}')
            git commit -m "chore: sync image tag from $SOURCE_BRANCH ($SYNCED_TAG) [skip ci]"
          else
            COMMIT_SHA=$(echo "${{ github.sha }}" | cut -c1-7)
            IMAGE_TAG="sha-${COMMIT_SHA}"
            git commit -m "chore: update image tag to $IMAGE_TAG [skip ci]"
          fi
          git push
        fi

  deploy-dev:
    needs: build-and-push
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/develop'

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Development Deployment Summary
      run: |
        echo "🚀 Development deployment triggered"
        echo "📦 Image: ghcr.io/pablodelarco/roomio:sha-$(echo ${{ github.sha }} | cut -c1-7)"
        echo "🎯 Namespace: roomio-dev"
        echo "🔄 ArgoCD will automatically sync the changes within 3 minutes"
        echo "🌐 Access: https://dev.roomiorentals.com"

    - name: Wait for ArgoCD sync
      run: |
        echo "⏳ Waiting 180 seconds for ArgoCD to sync..."
        sleep 180

    - name: Run smoke tests on development
      run: |
        chmod +x scripts/smoke-test.sh
        ./scripts/smoke-test.sh https://dev.roomiorentals.com

    - name: Notify deployment success
      if: success()
      run: |
        echo "✅ Development deployment successful!"
        echo "🧪 All smoke tests passed"

  deploy-staging:
    needs: build-and-push
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/staging'

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Staging Deployment Summary
      run: |
        echo "🚀 Staging deployment triggered"
        echo "📦 Image: ghcr.io/pablodelarco/roomio:sha-$(echo ${{ github.sha }} | cut -c1-7)"
        echo "🎯 Namespace: roomio-staging"
        echo "🔄 ArgoCD will automatically sync the changes within 3 minutes"
        echo "🌐 Access: https://staging.roomiorentals.com"

    - name: Wait for ArgoCD sync
      run: |
        echo "⏳ Waiting 180 seconds for ArgoCD to sync..."
        sleep 180

    - name: Run smoke tests on staging
      run: |
        chmod +x scripts/smoke-test.sh
        ./scripts/smoke-test.sh https://staging.roomiorentals.com

    - name: Notify deployment success
      if: success()
      run: |
        echo "✅ Staging deployment successful!"
        echo "🧪 All smoke tests passed"
        echo "📋 Ready for production promotion"

  deploy-prod:
    needs: build-and-push
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    environment:
      name: production
      url: https://roomiorentals.com

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Production Deployment Summary
      run: |
        echo "🚀 Production deployment triggered"
        echo "📦 Image: ghcr.io/pablodelarco/roomio:sha-$(echo ${{ github.sha }} | cut -c1-7)"
        echo "🎯 Namespace: roomio-prod"
        echo "🔄 ArgoCD will automatically sync the changes within 3 minutes"
        echo "🌐 Access: https://roomiorentals.com"

    - name: Wait for ArgoCD sync
      run: |
        echo "⏳ Waiting 180 seconds for ArgoCD to sync..."
        sleep 180

    - name: Run smoke tests on production
      run: |
        chmod +x scripts/smoke-test.sh
        ./scripts/smoke-test.sh https://roomiorentals.com

    - name: Notify deployment success
      if: success()
      run: |
        echo "✅ Production deployment successful!"
        echo "🧪 All smoke tests passed"
        echo "🎉 New version is live!"
